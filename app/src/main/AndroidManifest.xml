<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.ac.skuniv.pushpush">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".IntroActivity">  <!--처음 시작 액티비티-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"> <!--intent할 activity들-->
        </activity>
        <activity
            android:name=".MainActivity2"> <!--intent할 activity들-->
        </activity>
        <activity
            android:name=".HowActivity"> <!--intent할 activity들-->
        </activity>
        <activity
            android:name=".RankingActivity"> <!--intent할 activity들-->
        </activity>
    </application>

</manifest>